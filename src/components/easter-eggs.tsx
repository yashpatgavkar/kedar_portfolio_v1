"use client";

import NyanCat from "./nyan-cat";

/**
 * Keeps Kedar's P-key paint-stroke interaction without the original template's
 * devtools detector and personal console messages.
 */
const EasterEggs = () => <NyanCat />;

export default EasterEggs;
